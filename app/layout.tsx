import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TaskRig - Intelligent AI Agents for Operational Excellence',
  description: 'Transform your operations with autonomous AI agents that automate complex workflows, optimize processes, and deliver measurable results.',
  keywords: 'AI agents, automation, workflow optimization, operational efficiency, task automation',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
